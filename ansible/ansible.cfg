# Configures Ansible defaults for running the CNS playbook.
[defaults]
inventory = ./inventory/hosts.ini
host_key_checking = False
interpreter_python = auto_silent
retry_files_enabled = False
roles_path = ./roles
timeout = 30

[ssh_connection]
pipelining = True
